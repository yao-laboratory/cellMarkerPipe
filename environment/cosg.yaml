name: COSGR_env
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python=3.9
  - r=4.1
  - r-seurat
  - bioconductor-dropletutils
  - scikit-learn
  - r-remotes
  - pip
  - pip:
    - pandas=1.5
    - scikit-learn
###R package###

# install.packages("remotes")
# remotes::install_github("genecell/COSGR")
